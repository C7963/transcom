# PSCAN 交接文档 — 前端不变频 / 中心杂散峰问题

- 日期：2026-08-14
- 涉及工程：`D:\work\8.12\BSPDriver`（C++ DLL）+ `D:\work\8.12\PscanTestClient`（Qt 客户端）
- 参考真源（C#）：`D:\work\3-ReceiverServer_WuWei_320M\3-ReceiverServer_WuWei_320M`（ReceiveDataService）
- 目标硬件：MZ121（ADC 采样率 FS_ADC = 1.2288GHz）

---

## 一、问题是什么（现象 → 根因）

### 1. 现象

| 操作 | 观察 |
|---|---|
| 无信号源，设中心频率 921.6M | 显示正中间有一个峰 |
| 无信号源，设中心频率 <100M | 全是噪音，无峰 |
| 无信号源，中心分别设 500M / 1.5G / 3G | 中心峰"跟着走"，总在正中间 |
| 信号源 500M @ -30dBm，中心 495M、span 20M | 峰仍在 ~495M（中心），**不在 500M**，且只高出噪底约 40dB |
| 信号源 80M，中心 80M | 全噪音 |

结论：**任何频率都收不到真实信号**。中心那个"峰"不是信号，是接收机自身的杂散。

### 2. 中心峰的本质

- 921.6MHz = FS_ADC(1.2288G) − 307.2M(扫频 NCO) = **MZ121 前端物理中频**。
- 这个峰只在混频通道（>180M）出现，在直通/低频通道（<180M，`Rf_Sub=180M`）不出现 → 它是**中频链自身的杂散**（IF 滤波器中心 921.6M 上的自噪声/泄漏），永远钉在显示中心，跟设置频率无关。
- 之前"峰跟随中心频率"会误判为"前端在调谐"，实际不是——该杂散在任何设置下都在中心。

### 3. 根因（代码层面）

C++ 的 PSCAN 初始化走的是**通用 `InitDevice` 路径**，而它缺了射频前端的三块关键初始化：

| 缺的东西 | 作用 | 后果 |
|---|---|---|
| **HMC703 config2（13 条）从未写** | 前端本振 LO 的 PLL 配置 | 本振不起振 → 混频器不工作 → 收不到任何真实信号 |
| **Si5386 在 `adc_status==OK` 时被跳过** | ADC 采样时钟 | 时钟配置看板子状态，时好时坏 |
| **扫频 NCO `0x000C0005/6` 从不写** | 把中频 921.6M 搬到分析窗 | 分析窗位置不对 |

对照 C# 参考：`InitBaseBand`（`DataService.cs:2362-2394`）**每次启动无条件**做全量初始化（HMC703 → Si5386 → ADC9695+JESD → 扫频 NCO），所以 C# 端一切正常。

---

## 二、解决思路

**目标**：让 C++ 初始化对齐 C# 接收机启动序列，每次启动无条件全量配置时钟+前端。

选的是**方案 A（放开已有代码，改动最小）**——工程里其实早就有一份对齐 C# 的初始化代码，只是被 `#if 0` 禁用了（注释写着"保留用于回滚"），把它放开并接通入口即可，而不是新写逻辑。

五步：

1. 放开被 `#if 0` 禁用的 `ConfigureMZ121PscanClock`（HMC703 13 条 + Si5386 566 条全量 + 校准序列）、`ConfigureMZ121PscanAdc`（ADC9695 + JESD + DMA）以及 ADC 表；
2. 放开 `InitPscanDevice`（把时钟→ADC 串起来，带 `pscanInitialized_` 防重入）；
3. `ConfigureMZ121PscanAdc` 补扫频 NCO 两行（`0x000C0005=0`、`0x000C0006=0x4000`）；
4. PSCAN 客户端入口 `PscanApi_Init` 从 `InitDevice()` 切到 `InitPscanDevice()`（只影响 PSCAN，不动其他模块）；
5. 参数对齐 C#：`Pscan_BW` 614.4e6→80e6、`RFRBW` 1000→10000。

---

## 三、代码修改清单

### 1. `CommonManager.cpp`
- 放开第一处 `#if 0`（原 :62-329）：恢复 `kMZ121AdcConfig2`（66 条）、`kMZ121JesdConfig`、`ConfigureMZ121PscanClock`、`ConfigureMZ121PscanAdc` 的编译。
- 放开第二处 `#if 0`（原 :419-463）：恢复 `InitPscanDevice`。
- `ConfigureMZ121PscanAdc` 内（`adc1_sync` 之后、`dma_reset` 之前）补两行：
  ```cpp
  ok = WritePscanAdcRegister(mem, 0x000C0005, 0x00000000, "sweep_nco_ftw_low") && ok;
  ok = WritePscanAdcRegister(mem, 0x000C0006, 0x00004000, "sweep_nco_ftw_high_307p2M") && ok;
  ```

### 2. `CommonManager.h`
- `public` 加 `bool InitPscanDevice();`（`InitDevice()` 之后）。
- `private` 加 `bool pscanInitialized_ = false;` 与 `std::mutex pscanInitMutex_;`。

### 3. `PscanApi.cpp:59-62`
- `common.InitDevice()` → 带失败处理的 `InitPscanDevice()`（失败 `return 0`）。

### 4. `Pscan.cpp`
- `:443` `context.Pscan_BW = 80.0e6;`（原 614.4e6，对齐 C# App.config BW=80E6）。
- `:446` `context.RFRBW = 10000;`（原 1000，对齐 C# mz121 RFRBW=10000）。

### 5. `PscanClockTables.h`（编译修复）
- HMC703/Si5386 两张表和两个 Count 常量都在 `namespace Common` 里，而 `CommonManager.cpp` 的匿名命名空间在全局作用域看不到它们：
  - 5 处引用加了 `Common::` 前缀（`CommonManager.cpp` 内的 range-for 和日志）；
  - 4 处 `static constexpr` → `inline constexpr`（C++/CLI `/clr` 下命名空间级 `static constexpr` 内部链接符号对引用点不可见的问题，C++17 `inline` 变量是标准解法）。

---

## 四、关键寄存器速查（供后续调试）

| 寄存器 | 值 | 含义 |
|---|---|---|
| `0x000C0005` | `0` | 扫频 ADC NCO FTW 低 32 位 |
| `0x000C0006` | `0x4000` | 扫频 ADC NCO FTW 高 16 位 → NCO=307.2MHz |
| `0x000C0007` | `3`（PScan/mz121） | ADC 抽取倍数（1.2288G/3 = 409.6M） |
| `0x000B01xx` | HMC703 config2（13 条） | 前端本振 PLL |
| `0x000B02xx` | Si5386 config4（566 条） | ADC 采样时钟 |
| `0x00006002/03` | 扫频表 64bit RF 频率 | FPGA 按表驱动前端 LO |
| `0x00006004` | 校准系数 | 幅值误差 |
| `0x00006001` | `0x80000000+i` 然后 `i` | 扫频表写入触发 |

---

## 五、当前状态与后续

### 状态
- 代码已改完（方案 A 全部 + 编译修复）。
- **待重新编译**：MSBuild 在 AI 环境被安全策略拦截，需人工在 VS 生成（`TransComApi.sln` → Debug x64）。
- 若还有编译报错，重点看 `CommonManager.cpp` 的匿名命名空间相关行。

### 远程验证方法（编译通过后）
1. 冷启动（断电重启）只跑 C++ 客户端；
2. 信号源 500M @ -30dBm，中心 495M、span 20M；
3. **预期：峰出现在 500M 位置（窗口 75% 处），而不是中心** → HMC703 起振、前端变频打通，问题闭环。

### 遗留（后续再处理）
- 幅值校准：`Pscan.cpp` 里 `PscanErrorValue = 0.0`（写死），公式常数 +107，未对齐真实 dBm。待"峰位置正确"后再校幅值。
